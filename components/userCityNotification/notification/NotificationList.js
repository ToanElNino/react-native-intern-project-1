import React from 'react';
import NotificationItem from './NotificationItem';

const RenderNotificationItem = ({item}) => {
  return <NotificationItem title={item} />;
};
export default RenderNotificationItem;
